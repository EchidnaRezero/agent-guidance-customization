interface:
  display_name: "When Checking PowerShell Command Safety"
  short_description: "Check PowerShell command safety with a subagent"
  default_prompt: "Use $when-checking-powershell-command-safety with a separate subagent before running a risky Windows PowerShell command."
