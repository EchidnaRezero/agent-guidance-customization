interface:
  display_name: "When Checking Parallel Command Safety"
  short_description: "Check parallel command safety with a subagent"
  default_prompt: "Use $when-checking-parallel-command-safety with a separate subagent before running planned commands in parallel."
