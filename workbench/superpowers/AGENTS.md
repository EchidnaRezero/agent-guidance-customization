This workbench is Codex-only.

- Home global policy lives above this workbench. See `home-global-agents/AGENTS.md` for the replacement home `agents.md` draft.
- Use `skills/using-superpowers/SKILL.md` as the entry workflow.
- If the current environment is not Windows, load the matching OS environment skill before following shell-heavy skills.
- Use `.codex/INSTALL.md` and `docs/README.codex.md` for Codex setup details.
- Non-Codex assets were removed from this workbench. See `docs/non-codex-recovery-guide_kr.md` if you need to restore support for another agent.
